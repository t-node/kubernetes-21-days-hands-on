# DELIBERATELY INVALID -- three separate schema violations.
#
#   number: 42        -> above `maximum: 10`
#   class: economy++  -> not in the `enum`
#   to: LA            -> shorter than `minLength: 3`
#
# The API server rejects this BEFORE storing anything, and names every
# violated constraint at once.
apiVersion: flights.example.com/v1
kind: FlightTicket
metadata:
  name: bad-ticket
  namespace: cka19
spec:
  from: Mumbai
  to: LA
  number: 42
  class: "economy++"
